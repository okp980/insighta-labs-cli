# Copy to `.env` in this directory or to `~/.insighta/.env` on your machine.
# Project `.env` in the current working directory overrides `~/.insighta/.env`.

INSIGHTA_API_URL=http://localhost:8000
# INSIGHTA_API_VERSION=1
